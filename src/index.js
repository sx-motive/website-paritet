import React from "react";
import ReactDOM from "react-dom";
import RoutInner from "./RoutInner";

import "./assets/app.css";

ReactDOM.render(
  <React.StrictMode>
    <RoutInner />
  </React.StrictMode>,
  document.getElementById("root")
);
